name: costeadorlibre
description: "Herramienta para calcular costos reales de productos"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  
  cupertino_icons: ^1.0.8
  
  # State Management
  provider: ^6.1.2
  
  # Persistencia
  path_provider: ^2.1.4
  
  # Import/Export
  file_picker: ^8.1.4
  
  # Utilidades
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true